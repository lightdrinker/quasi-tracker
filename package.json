{
  "name": "quasi-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Daily tracker for Korean quasi-drug product permission data.",
  "scripts": {
    "build": "node -e \"console.log('Static app ready')\"",
    "dev": "node server.js",
    "start": "node server.js",
    "snapshot": "node scripts/build-snapshot.mjs",
    "check": "node --check server.js && node --check api/qdrg.js && node --check api/refresh.js && node --check public/app.js && node --check scripts/build-snapshot.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
